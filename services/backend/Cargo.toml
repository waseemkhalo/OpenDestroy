[package]
name = "dictation-backend"
version.workspace = true
edition.workspace = true
license.workspace = true
[dependencies]
axum = { version = "0.8", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json", "multipart", "stream"] }
tokio-tungstenite = { version = "0.29", features = ["rustls-tls-native-roots"] }
futures-util = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
uuid = { version = "1", features = ["serde", "v4"] }
sha2 = "0.10"
aes-gcm = { version = "0.10", features = ["zeroize"] }
# Zeroize the expanded cipher and authentication state as well as owned key buffers.
aes = { version = "0.8", features = ["zeroize"] }
ghash = { version = "0.5", features = ["zeroize"] }
polyval = { version = "0.6", features = ["zeroize"] }
zeroize = "1"
rand = "0.8"
rusqlite = { version = "0.32", features = ["bundled"] }
regex-lite = "0.1"
urlencoding = "2"
tracing = "0.1"
chrono = "0.4"
dictation-protocol = { path = "../../crates/dictation-protocol" }
[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
http-body-util = "0.1"
